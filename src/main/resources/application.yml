spring:
  application:
    name: garbage_recycling
  datasource:
    url: jdbc:mysql://8.134.191.200/garbage_recycling?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: Mini-backend1001
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      idle-timeout: 30000
      connection-timeout: 20000
      max-lifetime: 1800000
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 8
          max-wait: -1ms
          max-idle: 8
          min-idle: 0
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

server:
  port: 8080
  servlet:
    context-path: /api

mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: com.stu.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

wechat:
  app-id: wx4f0e28dcebba0909
  app-secret: 94c634118718272a9bacf52f1921e298
  auth-url: https://api.weixin.qq.com/sns/jscode2session?appid={0}&secret={1}&js_code={2}&grant_type=authorization_code
  test:
    mode: false

jwt:
  secret: dGhpcy1pcy1hbGwtbGVnYWwtYmFzZTY0LXNlY3JldC1rZXk=
  expiration: 86400000
  header: Authorization

app:
  upload-dir: ./uploads
  images:
    category-path: ${app.upload-dir}/category-images
    identity-bg-path: ${app.upload-dir}/identity-bg

logging:
  level:
    com.stu.mapper: debug
    org.springframework.web: info
    com.stu: debug
    com.recycle: debug